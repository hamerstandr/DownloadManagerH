<Window x:Class="DownloadManagerH.Windows.Dialog.CustomInputBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ورودی" Height="230" Width="400" 
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="lblPrompt" FontSize="16" FontWeight="Bold"/>
        <TextBox x:Name="txtInput" Grid.Row="1" FontSize="15"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button ToolTip="تایید" Width="90" Margin="0,0,10,0" Click="BtnOk_Click" Style="{StaticResource ToolbarButtonStyle}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Image Width="20" Height="20" Source="{StaticResource CheckIcon}"/>
                    <TextBlock Text="تایید" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>
            <Button ToolTip="انصراف" Width="90" Click="BtnCancel_Click" Style="{StaticResource ToolbarButtonStyle}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Image Width="20" Height="20" Source="{StaticResource CrossIcon}"/>
                    <TextBlock Text="انصراف" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window> 