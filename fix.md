# فهرست مشکلات و بدهی‌های فنی DownloadManagerH

> این فایل پس از مرور ساختار پروژه، فایل پروژه، کدهای اصلی C#/WPF، افزونه‌های مرورگر و مستندات ایجاد شده است. موارد زیر به‌صورت چک‌لیست شماره‌دار آمده‌اند تا بتوان آن‌ها را مرحله‌به‌مرحله پیگیری و رفع کرد.

1. [x] **ناسازگاری نسخه .NET در مستندات و پروژه:** در README پیش‌نیاز و مسیر خروجی بر اساس `.NET 9` توضیح داده شده، اما فایل پروژه `net10.0-windows7.0` را هدف گرفته است. این موضوع باعث سردرگمی کاربر و خطا در محیط‌هایی می‌شود که فقط .NET 9 دارند.

2. [x] **عدم امکان اجرای بیلد در محیط فعلی:** دستور `dotnet build DownloadManagerH/DownloadManager.sln` به دلیل نبودن `dotnet` در محیط اجرا نشد. باید زنجیره build/test در CI یا مستندات راه‌اندازی محیط توسعه شفاف شود.

3. [x] **Target Framework مشکوک و ریسکی:** مقدار `net10.0-windows7.0` برای یک برنامه WPF ویندوزی با README مبتنی بر ویندوز ۱۰/۱۱ و .NET 9 هم‌خوان نیست. لازم است نسخه هدف پایدار، پشتیبانی‌شده و مطابق مستندات انتخاب شود.

4. [x] **فایل‌های manifest نمونه Native Messaging مسیر ثابت و Extension ID نمونه دارند:** manifestهای موجود در `NativeMessagingManifests` مسیر `C:\Program Files\DownloadManagerH\DownloadManager.exe` و شناسه‌هایی مثل `your-chrome-extension-id` دارند. در نصب واقعی افزونه‌ها کار نمی‌کنند مگر اینکه در زمان نصب/ثبت‌نام به‌صورت خودکار با مسیر و شناسه واقعی جایگزین شوند.

5. [x] **شناسه‌های پیش‌فرض Native Messaging هنوز placeholder هستند:** پیکربندی پیش‌فرض برای Chrome/Edge/Firefox شامل مقدارهای placeholder است؛ در نتیجه ارتباط Native Messaging بعد از نصب عادی احتمالاً برقرار نمی‌شود تا کاربر دستی آن‌ها را اصلاح کند.

6. [x] **CORS بیش‌ازحد باز در API افزونه‌ها:** سرور افزونه در پاسخ‌ها `Access-Control-Allow-Origin: *` می‌دهد. حتی اگر روی localhost اجرا شود، بهتر است فقط origin افزونه‌های مجاز پذیرفته شود تا هر صفحه وب محلی/مرورگری نتواند درخواست ارسال کند.

7. [x] **API محلی افزونه‌ها احراز هویت ندارد:** endpointهایی مثل افزودن دانلود، وضعیت و تنظیمات ظاهراً بدون توکن، امضا یا اعتبارسنجی origin واقعی قابل فراخوانی هستند. این موضوع می‌تواند باعث افزودن دانلود ناخواسته یا سوءاستفاده از سرویس محلی شود.

8. [x] **Rate limit بر اساس IP برای localhost کافی نیست:** محدودسازی درخواست‌ها فقط بر اساس `RemoteEndPoint.Address` انجام می‌شود؛ برای همه درخواست‌های محلی معمولاً یک IP مشترک وجود دارد و این روش هم برای امنیت ناکافی است و هم ممکن است کاربران/افزونه‌های معتبر را اشتباهاً محدود کند.

9. [ ] **استفاده از `Task.Run` بدون نگهداری/await برای loopها و درخواست‌ها:** در چند بخش، taskهای پس‌زمینه fire-and-forget ساخته شده‌اند. خطاهای داخلی ممکن است گم شوند، shutdown تمیز سخت شود و race condition ایجاد شود.

10. [ ] **استفاده از `.Wait()` در مسیرهای UI/Shutdown:** چند کلاس برای سازگاری قدیمی از `.Wait()` استفاده می‌کنند. این کار در WPF می‌تواند deadlock، فریز UI یا توقف ناقص برنامه ایجاد کند. بهتر است APIها کاملاً async شوند.

11. [ ] **وجود `async void` خارج از event handlerهای استاندارد:** چند متد داخلی و callback تایمر با `async void` نوشته شده‌اند. در صورت exception کنترل‌نشده، برنامه ممکن است crash کند یا خطا لاگ نشود.

12. [x] **Converterها در `ConvertBack` استثنای NotImplementedException می‌اندازند:** `CompletedOnlyConverter` و `GroupColorConverter` در bindingهای TwoWay یا خطاهای XAML می‌توانند crash ایجاد کنند. بهتر است `Binding.DoNothing` یا `DependencyProperty.UnsetValue` برگردانند.

13. [ ] **catchهای خالی وجود دارد:** چند نقطه خطاها را بدون لاگ یا پیام می‌بلعند. این موضوع عیب‌یابی مشکلات دانلود، clipboard، theme و UI را سخت می‌کند.

14. [ ] **باز کردن فایل‌ها با `Process.Start` و آرگومان‌های shell نیازمند اعتبارسنجی دقیق‌تر است:** مسیر فایل‌ها و فرمان‌هایی مثل `rundll32.exe shell32.dll,OpenAs_RunDLL ...` باید quote/escape و اعتبارسنجی شوند تا مسیرهای عجیب یا ورودی آلوده مشکل امنیتی/عملیاتی ایجاد نکند.

15. [x] **مدیریت Startup از `Assembly.Location` استفاده می‌کند:** در برنامه‌های publish شده single-file یا برخی حالت‌های .NET، `Assembly.GetExecutingAssembly().Location` ممکن است خالی یا نامناسب باشد. بهتر است از `Environment.ProcessPath` با fallback مطمئن استفاده شود.

16. [x] **پورت‌ها و مستندات یکپارچه نیستند:** README برای افزونه‌ها و TrafficWatch به پورت `9090` اشاره می‌کند، اما `DownloadListener`/`PluginApiServer` به‌صورت پیش‌فرض روی `24680` تعریف شده‌اند. این ناسازگاری باعث شکست ارتباط افزونه‌ها/داشبورد می‌شود.

17. [ ] **افزونه‌های Chrome/Edge/Firefox کدهای تکراری زیادی دارند:** فایل‌های افزونه در پوشه‌های مرورگرها تقریباً ساختار مشابه دارند. نبودن منبع مشترک یا build script، نگهداری و رفع باگ را سخت و احتمال drift بین مرورگرها را زیاد می‌کند.

18. [ ] **وجود پوشه `IDMEdgeExt` با کد minified و نام/دارایی‌های IDM:** این پوشه ظاهراً افزونه/کد minified مربوط به IDM را شامل می‌شود و از نظر نگهداری، شفافیت، مجوز و امنیت باید فوراً بررسی شود. اگر وابسته خارجی است باید منبع، مجوز و دلیل وجود آن مستند شود؛ در غیر این صورت حذف شود.

19. [ ] **نبود تست واحد/یکپارچه‌سازی استاندارد قابل اجرا:** فایل‌هایی با نام test داخل پروژه اصلی دیده می‌شود، اما ساختار تست جداگانه و runner استاندارد مشخص نیست. بهتر است پروژه تست مستقل با xUnit/NUnit/MSTest ایجاد شود.

20. [ ] **کدهای تست/ابزار توسعه داخل اسمبلی برنامه اصلی قرار گرفته‌اند:** کلاس‌هایی مثل `ThemeManagerTests`, `HistoryManagerTests`, `ResourceManagementTests` و پنجره تست منابع در پروژه اصلی هستند. این موارد بهتر است به پروژه تست یا ابزار debug-only منتقل شوند.

21. [x] **استفاده از MD5 برای checksum در BackupManager:** MD5 برای امنیت مناسب نیست. اگر checksum فقط برای تشخیص تغییرات غیرامنیتی است باید در کد/مستندات صریح شود؛ در غیر این صورت از SHA-256 یا الگوریتم مناسب‌تر استفاده شود.

22. [ ] **قفل‌گذاری و thread-safety در مدیریت دانلودها باید بازبینی شود:** موتور دانلود هم‌زمان، connection pool، pause/resume و eventهای UI را ترکیب می‌کند. لازم است race conditionها، dispose هم‌زمان و تغییر وضعیت دانلودها زیر بار واقعی تست شوند.

23. [x] **اعتبارسنجی URLهای ورودی افزونه‌ها و clipboard باید سخت‌گیرانه‌تر شود:** باید schemeهای مجاز، اندازه URL، redirectها، localhost/private network policy و نام فایل استخراج‌شده کنترل شوند تا SSRF/دانلود ناخواسته/نام فایل خطرناک رخ ندهد.

24. [ ] **مدیریت خطا و retry دانلودها نیازمند تست end-to-end است:** منطق retry، recovery، چندبخشی و ادغام فایل باید برای قطع شبکه، تغییر Content-Length، نبود Range support و پاسخ‌های 4xx/5xx پوشش تست داشته باشد.

25. [ ] **ذخیره‌سازی تنظیمات و تاریخچه نیازمند migration/versioning است:** کلاس‌های تنظیمات، تاریخچه و backup فایل‌های JSON/XML را ذخیره می‌کنند، اما استراتژی نسخه‌بندی، migration و recovery از فایل خراب باید کامل‌تر و مستند شود.

26. [ ] **مسیرهای ذخیره‌سازی، دسترسی فایل و conflict handling باید امن‌تر شوند:** باید traversal، نام فایل‌های نامعتبر ویندوز، طول مسیر، فایل‌های قفل‌شده، overwrite ناخواسته و permission errorها پوشش داده شوند.

27. [ ] **لاگ‌ها ممکن است اطلاعات حساس ذخیره کنند:** URLهای دانلود می‌توانند token/query string خصوصی داشته باشند. باید masking برای queryهای حساس، headerها و مسیرهای خصوصی اضافه شود.

28. [ ] **پشتیبانی RTL/LTR و چندزبانه در کد کامل تفکیک نشده است:** README ادعای پشتیبانی فارسی/عربی/انگلیسی دارد، اما بسیاری از متن‌ها به‌صورت hard-coded فارسی در کد دیده می‌شوند. بهتر است همه رشته‌ها به resource file منتقل شوند.

29. [ ] **مستندات نصب افزونه‌ها با Native Messaging کامل نیست:** نصب unpacked افزونه کافی نیست؛ ثبت manifest و شناسه extension واقعی هم لازم است. مستندات باید مرحله ثبت Native Messaging و عیب‌یابی اتصال را دقیق توضیح دهد.

30. [ ] **نبود CI/CD و کنترل کیفیت خودکار در مخزن:** باید build، test، format/analyzer، بررسی XAML و بسته‌بندی افزونه‌ها در pipeline خودکار اجرا شود تا ناسازگاری‌هایی مثل نسخه .NET، placeholderها و خطاهای کامپایل زود تشخیص داده شوند.

31. [ ] **وابستگی به Windows Registry در چند نقطه بدون abstraction کافی:** برای Startup و Native Messaging رجیستری مستقیم استفاده شده است. بهتر است abstraction/test seam ایجاد شود تا تست‌پذیری و مدیریت خطا بهتر شود.

32. [ ] **مدیریت lifecycle سرورهای محلی باید یکپارچه شود:** Plugin API، Native Messaging host، listenerها و named pipe server هرکدام start/stop جدا دارند. لازم است ترتیب راه‌اندازی/خاموشی، cancellation token و dispose مرکزی تعریف شود.

33. [ ] **سیاست امنیتی افزونه‌ها و permissionها باید بازبینی شود:** manifestهای افزونه و background/content scriptها باید از نظر حداقل permission، host permissions، CSP و دسترسی به تب‌ها بررسی و محدود شوند.

34. [ ] **فایل‌های باینری/آیکون و منابع تکراری باید پاکسازی شوند:** چند آیکون و resource مشابه وجود دارد. لازم است منابع استفاده‌نشده حذف و نام‌گذاری assets استاندارد شود.

35. [x] **نام‌گذاری و ساختار پروژه نیازمند تمیزکاری است:** مسیر ریشه شامل `DownloadManagerH/DownloadManager` است، اما README در بخش‌های مختلف به `DownloadManager/…` اشاره می‌کند. ساختار و مسیرهای مستندات باید یکسان شوند.
